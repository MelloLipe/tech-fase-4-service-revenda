name: CD

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout codigo
        uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t servico-principal .

      - name: Deploy (placeholder)
        run: echo "Deploy do servico principal concluido com sucesso"
